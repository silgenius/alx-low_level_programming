#include "main.h"
#include <string.h>

/**
 * _strcmp - a function that compares two strings.
 *
 * @s1: first char
 * @s2: second char
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
