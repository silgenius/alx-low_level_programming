#include <stdio.h>
#include "main.h"

/**
 * print_numbers - check the code
 *
 * Return: Always 0.
 */

void print_numbers(void)
{
	char ch = 65;

	for (ch = 65; ch <= 74; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
}
