#include<stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: char data type of the of the function
 *
 * Return: returns (0)
 */

int _strlen(char *s)
{
	return (strlen(s));
}
