#include "main.h"

/**
 * add jack_bauer - Entry point
 *
 * Description:  a function that
 * adds two integers and returns the result.
 * Return: Always 0 (Success)
 */
int add(int x, int y)
{
	return (x + y);
}
