#include "main.h"

/**
 * cap_string - a function that changes all lowercase letters of a string to uppercasechar *string_toupper(char *)
 * 
 * @s: str
 * Return: char
 */
char *cap_string(char *s)
{
	for (s[i]
