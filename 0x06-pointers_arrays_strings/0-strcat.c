#include <string.h>
#include "main.h"

/**
 * _strcat - concatenate 2 char
 * @dest: first char
 * @src: second char
 *
 * Return: the result
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
